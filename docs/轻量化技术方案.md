# 围棋棋谱管理与 AI 复盘工具轻量化技术方案

## 1. 方案概述

采用“单机桌面应用 + 本地 KataGo 进程 + SQLite 数据库”的轻量架构。系统无需部署云端服务，可离线完成棋谱管理、浏览编辑、AI 复盘、可视化分析、检索统计以及数据备份恢复。

设计目标：

- 安装和使用简单，尽量减少外部依赖。
- 本地保存棋谱及分析数据，保护用户隐私。
- 优先满足个人棋谱管理场景，不引入微服务和搜索集群。
- 模块边界清晰，为后续扩展同步、多人协作等能力保留空间。

## 2. 技术选型

| 层级 | 推荐技术 | 用途 |
| --- | --- | --- |
| 桌面框架 | Tauri 2 | 构建体积小、资源占用低的桌面应用，并提供文件及进程访问能力 |
| 前端框架 | Vue 3 + TypeScript + Vite | 构建棋谱库、编辑器和分析页面 |
| UI 组件 | Naive UI | 快速实现桌面管理界面 |
| 棋盘渲染 | HTML Canvas | 绘制棋盘、棋子、标记、变化图和热力图 |
| 数据图表 | ECharts | 绘制胜率曲线、目差曲线和关键手标记 |
| 本地核心 | Rust + Tauri Commands | 处理文件、数据库、备份以及 KataGo 子进程 |
| 数据存储 | SQLite + FTS5 | 保存元数据、标签、统计、分析结果并提供全文检索 |
| AI 引擎 | KataGo Analysis Engine | 提供胜率、目差、推荐着法及候选变化 |
| 测试 | Vitest、Playwright、Rust Test | 覆盖 SGF 解析、编辑逻辑和关键用户流程 |

## 3. 系统架构

```text
Vue 桌面界面
 ├─ 棋谱库与搜索
 ├─ Canvas 棋盘编辑器
 ├─ AI 分析面板
 └─ ECharts 分析图表
          │ Tauri IPC
          ▼
本地应用核心
 ├─ SGF 解析与编辑
 ├─ SQLite 数据访问
 ├─ 文件导入与导出
 ├─ 备份与恢复
 └─ KataGo 进程管理
       ├─ SQLite 数据库
       ├─ SGF 文件
       └─ KataGo 模型及分析缓存
```

应用界面与系统能力通过 Tauri IPC 通信。KataGo 作为独立子进程运行，应用通过标准输入输出提交分析任务和读取分析结果。

## 4. 功能设计

### 4.1 棋谱管理

- 支持单个或批量导入 SGF，解析棋手、赛事、日期、结果等属性。
- SGF 原始内容保存在应用数据目录，SQLite 保存索引和元数据。
- 文件夹采用数据库中的逻辑目录，避免因分类操作频繁移动实体文件。
- 标签使用多对多关联表管理。
- 删除默认采用软删除并进入应用回收站，降低误删风险。
- 重命名主要修改显示名称，避免破坏内部文件引用。
- 导出时根据当前棋谱树生成标准 SGF 文件。

### 4.2 棋谱浏览与编辑

棋谱在内存中使用树结构表示，每个节点对应一步着法或一个 SGF 属性节点：

```ts
interface MoveNode {
  id: string
  parentId?: string
  color?: "B" | "W"
  point?: string
  comment?: string
  marks: Mark[]
  children: MoveNode[]
}
```

- 主线和变化分支统一保存在节点树中。
- 前进、后退和跳转通过当前节点到根节点的路径重建棋盘状态。
- 自动播放由前端定时器驱动，并允许调整播放速度。
- 支持圆圈、三角、文字等 SGF 标记以及节点评论。
- 插入或删除着法后，重新校验受影响分支的落子合法性。
- 编辑期间修改保存在内存，用户保存时统一序列化为 SGF。
- 使用简单的命令栈提供撤销和重做，不引入复杂编辑框架。

### 4.3 KataGo AI 复盘

- 首次使用时由用户指定 KataGo 可执行文件、配置文件和模型路径。
- 分析开始时启动一个 KataGo 子进程，应用退出或空闲时安全关闭。
- 支持分析整局棋谱或指定手数范围。
- 每手保存胜率、目差、推荐着法、候选变化和访问次数。
- 使用“棋谱哈希 + KataGo 配置哈希 + 手数”作为缓存键，避免重复分析。
- 首版采用单队列串行执行分析任务，控制 CPU、GPU 和内存占用。
- 提供任务进度、取消、超时和 KataGo 异常重启机制。

### 4.4 可视化分析

- 使用 ECharts 绘制胜率曲线和目差曲线。
- 相邻两手胜率下降超过配置阈值时标记为关键手，默认阈值可设为 10%。
- 点击曲线数据点时，棋盘同步跳转到对应手数。
- 根据 KataGo 候选点数据，在 Canvas 棋盘上叠加热力图。
- 按胜率损失或目差损失划分好手、疑问手和恶手，阈值允许用户配置。

### 4.5 搜索与筛选

- 棋手、赛事、日期、标签、文件名使用 SQLite 普通字段及索引查询。
- SGF 评论、棋手、赛事等文本同步写入 FTS5 虚拟表，提供全文检索。
- 多个筛选条件由本地 SQL 动态组合，无需引入 Elasticsearch。
- 导入或编辑棋谱后，在同一事务内同步更新检索索引。

### 4.6 棋谱库统计

- 使用 SQLite 聚合查询计算对局总数、黑白胜率、常用布局等基础指标。
- 常用布局可按前若干手生成标准化布局键后进行分组统计。
- 首版采用实时计算；只有数据规模明显增大时才增加统计缓存表。

### 4.7 备份与恢复

备份使用 ZIP 文件，结构如下：

```text
backup.zip
 ├─ database.sqlite
 ├─ sgf/
 └─ manifest.json
```

- 备份前暂停写入并执行 SQLite checkpoint，保证数据一致性。
- `manifest.json` 记录应用版本、数据库版本、备份时间和文件校验信息。
- 恢复前校验备份版本及完整性。
- 恢复时先保留当前数据副本，成功后再切换，避免恢复失败导致数据丢失。

## 5. 核心数据模型

| 数据表 | 主要用途 |
| --- | --- |
| `games` | 棋谱名称、文件位置、哈希、棋手、赛事、日期、结果及 SGF 内容 |
| `folders` | 逻辑文件夹及父子关系 |
| `tags` | 标签名称及颜色等属性 |
| `game_tags` | 棋谱和标签的多对多关系 |
| `analysis_tasks` | 分析任务、进度、状态和错误信息 |
| `move_analysis` | 每手胜率、目差、推荐着法及候选变化 |
| `game_search` | 基于 FTS5 的全文检索索引 |
| `settings` | KataGo 路径、模型路径、分析参数及界面设置 |
| `trash` | 软删除记录及原始位置 |

为保持方案轻量，首版不将每个棋谱节点拆成数据库记录。数据库保存 SGF 文本，打开棋谱时解析成内存树，保存时重新序列化。

## 6. 关键流程

### 导入流程

1. 用户选择一个或多个 SGF 文件。
2. 后端读取并校验文件格式，计算内容哈希用于去重。
3. 解析棋谱元数据，将 SGF 复制到应用数据目录。
4. 在一个数据库事务中写入棋谱记录和全文检索索引。
5. 返回导入成功、重复或失败的明细。

### AI 分析流程

1. 前端提交棋谱及分析范围。
2. 后端检查已有缓存，生成待分析手数列表。
3. KataGo 任务按顺序执行，并持续向前端推送进度。
4. 每手结果增量写入 SQLite，任务中断后可继续分析。
5. 前端刷新曲线、关键手和棋盘热力图。

## 7. 性能与可靠性策略

- 棋谱列表分页加载，避免一次渲染大量记录。
- SGF 导入和 KataGo 分析在后台线程执行，避免界面卡顿。
- 分析结果分批写入数据库，兼顾实时展示和磁盘写入效率。
- 对数据库启用 WAL 模式，并对常用筛选字段建立索引。
- 对损坏 SGF、非法着法、KataGo 启动失败和模型不匹配提供明确提示。
- 日志按大小轮转，仅记录必要的运行及错误信息。

## 8. 测试策略

- SGF 解析测试：主线、分支、标记、评论、转义和异常文件。
- 编辑测试：插入、删除、分支切换、撤销重做及序列化回读。
- 规则测试：提子、打劫、自杀手和棋盘边界。
- 数据库测试：导入去重、筛选、全文检索、软删除及恢复。
- KataGo 通信测试：正常结果、超时、取消、进程退出及缓存命中。
- 端到端测试：导入棋谱、浏览编辑、启动分析、查看图表和导出棋谱。

## 9. 实施阶段

1. **基础版本**：完成 SGF 导入、解析、棋盘显示、播放和 SQLite 棋谱库。
2. **编辑版本**：完成分支、标记、评论、插入删除、撤销重做和导出。
3. **AI 版本**：完成 KataGo 配置、单任务分析、进度展示和结果缓存。
4. **分析版本**：完成胜率/目差曲线、关键手和棋盘热力图。
5. **管理版本**：完成组合搜索、统计、备份恢复和安装包优化。

## 10. 风险与控制

- **SGF 兼容性**：优先支持 FF[4] 常见属性，未知属性原样保留，避免保存后信息丢失。
- **编辑合法性**：棋盘状态必须由节点路径重放得到，并为复杂规则建立专门测试集。
- **KataGo 环境差异**：启动前检测可执行文件、模型和计算后端，失败时展示原始错误摘要。
- **AI 资源占用**：默认限制并发为一个任务，分析强度提供低、中、高预设。
- **数据安全**：使用事务、软删除、自动备份及恢复前校验，避免单点写入失败造成损坏。

## 11. 首版范围建议

首版聚焦单机个人使用，暂不实现账户系统、云同步、多端协作、插件系统和远程 AI 服务。该范围能够覆盖产品需求中的核心使用闭环，同时保持安装、开发和维护成本较低。
